{
  "name": "solpoker-phase0-private-counter",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "deploy": "anchor deploy --provider.cluster https://rpc.magicblock.app/devnet",
    "//test": "NODE_OPTIONS disables Node 24+/25 native TS stripping, which otherwise loads .ts as ESM and breaks extensionless imports before ts-node sees them.",
    "test": "NODE_OPTIONS=--no-experimental-strip-types TS_NODE_PROJECT=./tsconfig.json ANCHOR_WALLET=${ANCHOR_WALLET:-$HOME/.config/solana/id.json} ANCHOR_PROVIDER_URL=${ANCHOR_PROVIDER_URL:-https://rpc.magicblock.app/devnet} mocha --require ts-node/register -t 1000000 \"tests/**/*.ts\""
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "@magicblock-labs/ephemeral-rollups-sdk": "0.14.3",
    "bs58": "^6.0.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "chai": "^5.2.0",
    "mocha": "^11.2.2",
    "ts-mocha": "^11.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
